# STATE (완료 스냅샷)

단계가 끝나면 그 단계를 한 줄로 갱신한다. 세션별 서술은 `HANDOFF.md`에 남긴다.

## 완료된 단계

- 프로젝트 골격: Spring Boot 4.1.0 / Java 21 애플리케이션 초기 생성, `DpgbApplicationTests` smoke test 존재.
- API 계약 정규화: `docs/api/openapi.yaml` 및 ADR-0001(`docs/api/decisions/0001-contract-normalization.md`) 수립.
- 개발 하네스 전환: 기존 `.kiro/steering` 산출물을 삭제하고 `AGENTS.md` + `.harness/*` 체계로 통합. DB는 PostgreSQL 단일 기준으로 확정, H2 제거 결정.
- API 계약 보완(CLIAR-10): `docs/api/openapi.yaml`에서 대응 필드 없는 `coverColors` 필터 제거, `language`/`coverUrl`/`bookNumber` 스키마 불일치 수정(ADR-0002). `docs/api/README.md`의 삭제된 `.kiro/steering` 문서 참조를 `AGENTS.md`/`.harness/ARCHITECTURE.md`로 정리. 오늘의 기분 추천은 범위 밖으로 확인. **(스크랩 범위 판단과 `language` 필드는 CLIAR-43(ADR-0003)에서 뒤집혔다 — 아래 참조.)**
- API 계약 재정의(CLIAR-43, ADR-0003): 담당 범위를 "단순 DB CRUD로 해결 가능한 부분"으로 재확인해 `docs/api/openapi.yaml`을 재작성했다. 장르(`genre`)·무드(`moodTags`)·`language` 필드와 관련 필터를 전 스키마에서 제거(알라딘 API가 언어 정보를 전혀 제공하지 않고 기능표에도 없어 함께 제거), 표지 OCR(`/books/ocr`)·AI 도서 분석(`/books/ai-analyze`) 엔드포인트 삭제(다른 MSA 컴포넌트 담당으로 확인), 외부 도서 검색을 알라딘 API 단일 소스로 한정(`ExternalBook`에서 `source`/`kdc`/`externalBookId` 제거, `author`는 알라딘의 역할 라벨 결합 문자열에서 이름만 정리해 반환, `totalPages`는 대부분 응답에 없어 선택 필드로 명시). `GET /api/v1/library/books`의 `sortBy`에 `PROGRESS` 추가, 필터는 `author`/`readingStatus`만 유지. 신규 리소스 2종 추가: 스크랩(Scrap, 책별 CRUD — 문장/페이지번호/메모), 동물 사서(Librarian 마스터 + 회원별 대표 사서 선택). 표지 이미지 교체(`replaceLibraryBookCover`)와 스크랩 이미지 교체(`replaceScrapImage`)는 한 차례 추가했다가, 둘 다 오브젝트 스토리지(S3) 연동이 필요해 "단순 DB CRUD" 범위를 벗어난다는 사용자 확인을 거쳐 계약에서 제거했다(스크랩의 `imageUrl` 필드도 함께 제거). 스크랩을 범위에 포함한 것과 `language` 제거는 각각 ADR-0002의 기존 결정("문장 OCR·감상·비밀 메모는 범위 밖", "language는 서버가 ko로 채운다")을 반전한 것 — 사유는 ADR-0003에 기록. `.harness/DOMAIN.md`에 Scrap/Librarian aggregate 규칙 추가, LibraryBook에서 `genre`/`moodTags`/`language` 제거. 도메인/영속성/컨트롤러 구현은 아직 시작하지 않았다 — `.harness/PLAN.md` 참조.
- 구현 계획 수립: 인증 기반 → 공통 계약 인프라 → LibraryBook 도메인/영속성 → Library CRUD → Reading Progress → Book Discovery(어댑터+스텁) → 계약 테스트 전수화 순서를 `.harness/PLAN.md`에 확정. 업무 규칙 문서 `.harness/DOMAIN.md` 신설(삭제된 `.kiro/steering/domain.md` 내용을 이 저장소 범위로 재정리), `CLAUDE.md` 단일 소유권 표에 반영.
- PostgreSQL 전환 + Gradle `integrationTest` 태스크 구성(CLIAR-26): `build.gradle`에서 H2 제거, `org.postgresql:postgresql`/Flyway(`flyway-core`, `flyway-database-postgresql`)/Testcontainers(`org.testcontainers:junit-jupiter`, `org.testcontainers:postgresql` 1.21.3 고정 — Spring Boot 4.1.0 BOM이 가리키는 2.0.5가 아직 Maven Central 미게시) 추가. `application.yaml`(공통) + `application-local.yaml`(Docker Postgres 기본값) + `application-prod.yaml`(env var, 기본값 없음) 프로필 분리, `spring.jpa.hibernate.ddl-auto: validate` + Flyway 활성화, 최초 baseline `db/migration/V1__init.sql`(빈 마이그레이션) 추가. 로컬 Postgres용 `docker-compose.yml` 신설. `integrationTest` source set/task를 새로 구성하고 `check`에 연결, `TestcontainersConfiguration`(`@ServiceConnection` + `withReuse(true)`)과 `IntegrationTestSupport`(`@SpringBootTest`) 기반 클래스를 `src/integrationTest`에 신설, 기존 `DpgbApplicationTests`를 `src/test`에서 `src/integrationTest`로 옮겨 `IntegrationTestSupport`를 상속하도록 전환(더 이상 DB 없이 컨텍스트가 뜰 수 없으므로). `./gradlew test`, `./gradlew integrationTest`(Docker Desktop 기동 후 실제 컨테이너로 검증 완료), `./gradlew check` 모두 통과 확인. `RepositoryIntegrationTestSupport`(`@DataJpaTest`)는 첫 Repository 테스트 작성 시점까지 보류.
- 공통 계약 인프라(CLIAR-29): `com.chc.dpgb.common.exception`에 `DomainException`(최상위) → status별 abstract 5종(`BadRequestException`/`ForbiddenException`/`NotFoundException`/`ConflictException`/`BadGatewayException`) → `openapi.yaml`의 stable error code별 concrete 예외 13종(2단 계층). `GlobalExceptionHandler`(`@RestControllerAdvice`)가 5개 abstract 타입 + 예기치 못한 예외(500, `INTERNAL_ERROR`)를 `ErrorResponse{code, message}`로 매핑. 401은 기존 `JwtAuthenticationEntryPoint`가 그대로 전담(범위 밖). `GlobalExceptionHandlerTest`(`@WebMvcTest`, `SecurityConfigTest` 패턴 재사용)로 6개 status 매핑 검증, `./gradlew test` 통과 확인. concrete 예외는 아직 도메인 패키지가 없어 우선 `common.exception`에 배치 — LibraryBook/Scrap/Librarian aggregate 구현 시점에 도메인 패키지로 옮길지 재검토.
- 인증 기반(CLIAR-28): AWS Cognito User Pool을 대상으로 Spring Security Resource Server(JWT) 구성. `com.chc.dpgb.security.SecurityConfig`가 `issuer-uri`(`AUTH_ISSUER_URI` env var, 기본값 없음) 기반 `JwtDecoder`, 전 endpoint 인증 필수 `SecurityFilterChain`, `AuthenticationEntryPoint` 빈을 한 곳에서 구성. Cognito Access Token에 `aud`가 없어 `com.chc.dpgb.security.jwt.TokenUseValidator`(`token_use==access`)와 `ClientIdValidator`(`client_id`가 `AUTH_APP_CLIENT_ID`와 일치)로 표준 audience 검증을 대체. `com.chc.dpgb.security.MemberIdResolver`가 `sub` 클레임을 memberId로 추출하는 단일 지점. 인증 실패(401)는 `com.chc.dpgb.security.JwtAuthenticationEntryPoint`가 `com.chc.dpgb.common.ErrorResponse{code: UNAUTHORIZED}`로 통일 응답. `JwtDecoder` 빈과 그 주입 지점을 모두 `@Lazy`로 표시해, 실제 Cognito User Pool 없이도(`AUTH_ISSUER_URI` 미설정) 앱 컨텍스트가 정상 기동하도록 함(첫 인증 요청 시점까지 discovery 네트워크 호출 지연) — `./gradlew integrationTest`로 이 상태에서 컨텍스트 기동을 실제로 확인. 단위 테스트(`TokenUseValidatorTest`/`ClientIdValidatorTest`/`MemberIdResolverTest`)와 `SecurityConfigTest`(`@WebMvcTest` + 테스트 전용 컨트롤러, `JwtDecoder`는 `@MockitoBean`)로 401/200 경로 검증. 실제 Cognito 토큰으로의 검증(decoder-validator 실동작)은 User Pool이 준비된 뒤 재검토 필요 — `.harness/BACKLOG.md`로 옮길 것.
- 서재 순서 필드 재설계(ADR-0004): 정수 `bookNumber`가 재정렬 시 전체 재번호가 필요하고 목록 조회에 전혀 반영되지 않던 문제를 확인해, LexoRank 방식의 불투명 문자열 `shelfRank`로 교체(`docs/api/openapi.yaml` v0.3.0). 순서 변경을 `updateLibraryBook`에서 분리해 전용 `PATCH /api/v1/library/books/{bookId}/order`(`reorderLibraryBook`, `beforeBookId`/`afterBookId` 이웃 지정)를 신설. `GET /api/v1/library/books`의 `sortBy` 기본값을 `SHELF_ORDER`로, `sortOrder` 기본값을 `ASC`로 변경하고 `LibraryBookSummary`에 `shelfRank` 노출 추가. `.harness/DOMAIN.md`에 `shelfRank` 규칙(유일성 제약, 삽입·rebalance 방식) 반영, `.harness/PLAN.md`(LibraryBook 도메인/영속성, Library CRUD API)에 구현 체크리스트 반영. 도메인/영속성/컨트롤러 구현은 아직 시작하지 않았다.
- `ReadingStatus` 제거(ADR-0005): CLIAR-31(LibraryBook 도메인/영속성) 구현 착수 시점에 사용자가 상태 필드 자체가 불필요하다고 확정. `docs/api/openapi.yaml`(v0.4.0)에서 `ReadingStatus` 스키마, `getLibraryBooks`의 `readingStatus` 필터, `UpdateReadingProgressResponse.readingStatus`를 제거하고 진도율(`progress`)만 남김. `.harness/DOMAIN.md`의 "페이지와 독서 상태" 절을 "페이지와 진도율"로 재정리(상태 매핑 규칙 삭제), `.harness/PLAN.md`(LibraryBook 도메인/영속성, Library CRUD API, Reading Progress API)에서 관련 체크리스트 문구 정리. 코드 구현 전 결정이라 별도 리팩터링 없이 처음부터 `ReadingStatus` 없이 설계한다.
- LibraryBook 도메인/영속성(CLIAR-31): `com.chc.dpgb.library` 패키지 신설. `LibraryBook`(JPA entity, aggregate root)이 `register`/`updateMetadata`/`updateProgress`/`changeShelfRank`에 불변식을 캡슐화(`totalPages>0`, `0<=currentPage<=totalPages`, 전체 페이지를 기존 현재 페이지보다 작게 줄이는 것 금지, 이전 페이지 정정 허용) — 위반 시 `IllegalArgumentException`(공통 `common.exception` 타입으로의 번역은 controller/service가 생기는 "Library CRUD API" 티켓에서 결정). `ShelfRank`는 Spring 비의존 LexoRank pure 유틸(`[0-9A-Za-z]` 62자, `initial`/`before`/`after`/`between`/`rebalancedSequence`) — 두 이웃 사이 공간이 128자를 넘어서게 되면 `ShelfRankExhaustedException`(내부 신호)을 던짐. 영속성은 포트/어댑터로 분리 — `LibraryBookRepository`(순수 인터페이스, Spring Data 비의존, 서비스 계층이 의존하는 도메인 메서드명: `save`/`findOwnedBook`/`findShelfOrderedByRank`/`findLastRanked`/`existsByIsbn`)와 `LibraryBookRepositoryJpaAdapter`(`@Repository`, package-private `LibraryBookJpaRepository`의 파생 쿼리 메서드로 위임)로 나눠, 서비스 계층이 JPA 쿼리 메서드명에 직접 결합되지 않게 했다. `LibraryBookJpaRepository`를 어댑터 클래스 내부 nested interface로 옮기는 것도 시도했으나 Spring Data JPA가 nested interface를 리포지토리 빈으로 스캔하지 못해(`NoSuchBeanDefinitionException` 실증) 되돌렸다 — package-private 최상위 인터페이스로 동일한 캡슐화 목표를 달성. `LibraryBook`의 getter는 Lombok `@Getter`로 생성(setter 없음, 상태 변경은 도메인 메서드로만). Flyway `V2__create_library_book.sql`로 `library_book` 테이블과 unique 제약(`member_id`+`shelf_rank`, `member_id`+`isbn` partial — isbn 있는 도서만, ADR-0007) 추가. `RepositoryIntegrationTestSupport`(`@DataJpaTest`+Testcontainers, 이 저장소 최초 사용)를 `ARCHITECTURE.md` 설계대로 신설하고 `LibraryBookRepositoryTest`(8개, unique 제약 위반 포함)로 검증. 단위 테스트 `ShelfRankTest`(10개)/`LibraryBookTest`(14개) 포함 `./gradlew check` 전체 통과 확인. 이 작업 중 `spring-boot-starter-flyway` 누락(CLIAR-26부터 Flyway가 실제로 한 번도 자동 실행되지 않던 결함)을 발견해 함께 수정 — 상세는 `.harness/DECISIONS.md` 참조. controller/service 배선과 `shelfRank` rebalance 오케스트레이션은 "Library CRUD API"로 이연.
- `updateLibraryBook`을 전체 필드 포함 방식으로 통일(ADR-0006): `UpdateLibraryBookRequest`(`docs/api/openapi.yaml` v0.5.0)가 7개 필드를 모두 `required`로 요구하도록 바뀌었다 — `isbn`/`publisher`/`publishedDate`/`coverUrl`는 `null`=삭제, `title`/`author`/`totalPages`는 `null` 불가(`Scrap.updateScrap`과 동일 규칙). `LibraryBook.updateMetadata(...)`(도메인 메서드)도 같은 규칙으로 맞춰 부분 수정(null-스킵) 로직을 제거했다. `.harness/DOMAIN.md`(LibraryBook aggregate 절에 규칙 추가), `.harness/PLAN.md`(Library CRUD API 체크리스트 문구 갱신) 반영, `LibraryBookTest`에 관련 테스트 4개 추가(총 14개).
- ISBN 없는 도서의 제목·저자 중복 판정 제거(ADR-0007): 서로 다른 책이어도 제목·저자가 우연히 같을 수 있어 서버가 강제하지 않기로 확정. `LibraryBook`에서 `normalizedTitle`/`normalizedAuthor` 필드·정규화 로직 제거, `LibraryBookRepository`/`LibraryBookJpaRepository`/`LibraryBookRepositoryJpaAdapter`에서 `existsBy...NormalizedTitleAndNormalizedAuthor` 계열 메서드 제거, `V2__create_library_book.sql`에서 해당 컬럼·unique 인덱스 제거(아직 배포 전이라 별도 마이그레이션 없이 V2 직접 수정). ISBN 있는 도서의 유일성 판정만 남는다. `docs/api/openapi.yaml`은 이 메커니즘을 wire 계약에 노출한 적이 없어 스키마 변경은 없음(ISBN 없이 재등록 시 409→201로 바뀌는 동작 변화만 있음). `LibraryBookRepositoryTest`에서 관련 테스트 1개 제거(9→8개), `./gradlew check` 재통과 확인.
- 책장(Shelf) 관리 API 계약 설계(ADR-0008): 사용자가 제시한 기능표(책장 생성/책 넣기/수정/삭제/책장별 목록 조회/책장 목록 조회)를 바탕으로 `docs/api/openapi.yaml`(v0.6.0)에 `Shelf` 태그와 신규 endpoint 5개(`createShelf`/`getShelves`/`updateShelf`/`deleteShelf`/`getShelfBooks`) + `moveLibraryBookToShelf`(책 이동)를 추가했다. 핵심 설계 결정은 `shelfRank`(ADR-0004)의 유일성 범위를 사용자 전역(`memberId`)에서 책장별(`shelfId`)로 좁힌 것 — "책장별 책 목록 조회"가 그 책장 안의 진열 순서를 보여줘야 자연스럽기 때문이며, 사용자에게 `AskUserQuestion`으로 확인받았다(범위 축소/기본 책장 get-or-create 자동 생성/기본 책장 이름 변경 허용, 셋 다 권장안 그대로 확정). `reorderLibraryBook`은 같은 책장 내 이웃만 허용하도록 설명을 보강했고, 책장 간 이동은 별도 endpoint로 분리했다. `CreateLibraryBookRequest`에 선택적 `shelfId`, `CreateLibraryBookResponse`/`LibraryBookSummary`/`LibraryBookDetailResponse`에 `shelfId` 노출, `getLibraryBooks`에 선택적 `shelfId` 필터(하위 호환 위해 생략 시 전체 책장 합산 유지)를 추가했다. `.harness/DOMAIN.md`에 `Shelf` aggregate 절 신설, `LibraryBook`/`shelfRank` 절을 책장 범위로 갱신, `docs/api/decisions/0008-shelf-management.md`(ADR-0008) 신설, `docs/api/README.md` ADR 목록 갱신. 스크립트로 `$ref` 무결성·중복 `operationId`·미사용 스키마/응답/파라미터 검증 완료(operationId 22개). 이 단계는 계약 수준 설계만 다뤘다 — 구현은 아래 "Library CRUD API + 책장(Shelf) 관리(CLIAR-32)" 참조.
- Library CRUD API + 책장(Shelf) 관리 구현(CLIAR-32): ADR-0008 계약을 실제 코드로 구현했다. `Shelf` aggregate(`create`/`rename`, `isDefault`는 서버 전용) + `ShelfRepository`(포트)/`ShelfJpaRepository`/`ShelfRepositoryJpaAdapter`(`LibraryBookRepository`와 동일 패턴), `ShelfService`(`getOrCreateDefaultShelf` — `(member_id) WHERE is_default` 부분 unique 인덱스로 동시성 보장, 위반 시 재조회, `createShelf`/`getShelves`/`updateShelf`/`deleteShelf`(내부 책을 기본 책장 맨 뒤로 이동 후 삭제)/`getOwnedShelf`(404→403 순서)). `LibraryBook`에 `shelfId` 필드와 `changeShelfId` 추가, `register()` 시그니처에 `shelfId` 반영. `V3__add_shelf_and_rescope_library_book.sql`로 `shelf` 테이블 신설, `library_book.shelf_id`(+FK) 추가, 기존 데이터 백필, `shelfRank` unique 제약을 `member_id` 전역→`shelf_id` 범위로 재조정(`V2`는 이미 `develop`에 병합돼 직접 수정하지 않고 새 마이그레이션으로 처리). `LibraryBookRepository`(포트)를 `findOwnedBook`(소유자 스코프 단일 조회) 대신 스코프 없는 `findById` + 서비스 계층의 명시적 소유자 비교로 교체했다 — 계약의 모든 관련 endpoint가 403(권한 없음)과 404(없음)를 별도 응답으로 구분하는데 `findOwnedBook`으로는 이 둘을 구분할 수 없었기 때문(설계 시점에 발견). 페이징/필터/정렬(`findPage`, `findPageOrderByProgress`)은 Querydsl 없이 Spring Data JPA `@Query` + `Pageable`의 `Sort` 자동 결합으로 처리하고, 저장 컬럼이 아닌 `PROGRESS` 정렬만 계산식 `ORDER BY`를 쓰는 전용 쿼리로 분리했다. `LibraryBookService`가 CRUD/`reorderLibraryBook`(이웃 탐색 후 `ShelfRank.between`, `ShelfRankExhaustedException` 시 `rebalancedSequence`로 재계산 후 재시도)/`moveLibraryBookToShelf`/`updateReadingProgress`를 구현하고, 도메인의 `IllegalArgumentException`을 각 상황에 맞는 `common.exception` 타입으로 번역한다. 신규 concrete 예외 6종(`InvalidReorderTargetException` — ADR-0004 이후 endpoint라 CLIAR-29 때 누락됐던 것을 이번에 발견해 추가, `InvalidShelfTargetException`/`InvalidShelfDataException`/`ShelfAccessDeniedException`/`ShelfNotFoundException`/`DefaultShelfCannotBeDeletedException`). `LibraryBookController`/`ShelfController`(`com.chc.dpgb.library.web`)가 `@AuthenticationPrincipal Jwt` + `MemberIdResolver`로 memberId를 얻는 기존 컨벤션을 따른다. DTO는 Bean Validation 없이 `openapi.yaml` 스키마 1:1 대응 record로만 구성했다 — 도메인 계층이 이미 필수/불변식을 검증하므로 서비스가 그 예외를 번역하는 방식으로 통일하고, 컨트롤러는 primitive 언박싱이 필요한 필드(totalPages/currentPage)의 null만 직접 체크한다. `getLibraryBooks`의 `author` 필터는 정확히 일치로, `createLibraryBook`에 잘못된/남의 `shelfId`를 보낸 경우는 계약에 전용 응답이 없어 기존 400(`INVALID_BOOK_DATA`)으로 처리하기로 사용자에게 확인받았다. 검증: `ShelfServiceTest`/`LibraryBookServiceTest`(Mockito, get-or-create 동시성·소유권 404/403·reorder 검증/rebalance·중복 409 등), `LibraryBookControllerTest`/`ShelfControllerTest`(`@WebMvcTest`), `LibraryBookRepositoryTest`(shelf 범위로 갱신)·`ShelfRepositoryTest`(신규) 포함 `./gradlew check`(Docker Desktop 기동, 실제 PostgreSQL) 전체 통과.

- Book Discovery API 구현(CLIAR-34): 사용자가 알라딘 API 자격 증명(TTBKey, `.env`의 `ALADIN_API_TTB_KEY`)을 확보해, 기존 계획(어댑터+스텁)을 건너뛰고 실제 알라딘 ItemSearch 연동을 바로 구현했다. `com.chc.dpgb.discovery` 패키지 신설 — `ExternalBook`(record, 포트가 반환하는 공용 표현), `BookDiscoveryClient`(포트), `BookDiscoveryService`(title/author 둘 다 없으면 400 `INVALID_SEARCH_PARAMETER`, 있으면 클라이언트에 위임). 실제 연동은 `com.chc.dpgb.discovery.aladin` 하위에 package-private로 격리 — `AladinBookDiscoveryClient`(`RestClient` 기반, `@Component` + `@Lazy`), `AladinSearchResponse`/`AladinItem`/`AladinSubInfo`(응답 DTO, `@JsonIgnoreProperties(ignoreUnknown = true)`), `AuthorNameNormalizer`(역할 라벨 제거 순수 유틸). 라이브 호출로 실제 알라딘 응답 형태를 확인해 설계에 반영했다: (1) 오류 시에도 HTTP 200에 `{"errorCode":..,"errorMessage":..}` 바디로 응답하므로 HTTP status가 아니라 JSON의 `errorCode` 존재 여부로 `AladinApiException`(502)을 판단해야 한다, (2) `subInfo.itemPage`(페이지 수)는 `OptResult=itemPage`를 요청해도 실질적으로 거의 항상 비어 있어 이전 결정(totalPages는 선택 필드)이 실제로 맞았음을 재확인, (3) `isbn13`이 항상 신뢰할 수 있는 13자리 숫자이고 `isbn`(10자리) 필드는 종종 "K"로 시작하는 비-ISBN 내부 코드라 `isbn13`을 우선 사용하고 실패 시에만 `isbn`으로 폴백한다. `title`/`author`가 둘 다 있으면 알라딘 ItemSearch가 두 조건의 AND 검색을 지원하지 않아 `QueryType=Keyword`(자유 검색)로, 하나만 있으면 `QueryType=Title`/`Author`로 정밀 검색한다. `RestClient.Builder` 자동구성이 `spring-boot-starter-webmvc`에 포함되지 않아 `spring-boot-starter-restclient`를 `build.gradle`에 추가했다(Boot 4.1 세분화 모듈 체계, Flyway 때와 동일 패턴). `AladinBookDiscoveryClient`가 자격 증명 없는 환경(CI, 다른 개발자)에서 앱 기동을 막지 않도록 `@Lazy`를 빈과 `BookDiscoveryService` 생성자 주입 지점 양쪽에 붙였다(CLIAR-28의 `JwtDecoder` 패턴과 동일 이유 — 빈 정의에만 붙이면 다른 곳에서 즉시 생성됨). 검증: `AuthorNameNormalizerTest`(순수 함수), `AladinBookDiscoveryClientTest`(`MockRestServiceServer`에 실제 캡처한 알라딘 응답 fixture로 매핑·에러·QueryType 분기 검증, 네트워크 미사용), `BookDiscoveryServiceTest`(Mockito), `BookDiscoveryControllerTest`(`@WebMvcTest`) 및 `ALADIN_API_TTB_KEY`를 설정하지 않은 상태의 `./gradlew check`(Docker Desktop 기동, 실제 PostgreSQL)로 앱이 정상 기동함을 재확인. `.env`는 `.gitignore`에 이미 등록되어 있고 이 저장소는 `.env`를 자동으로 읽지 않는다 — 로컬 실행 시 사용자가 직접 셸/IDE 실행 설정에 `ALADIN_API_TTB_KEY`를 주입해야 한다.

- 자바 코드 전체 포맷팅 및 스타일 정책 확정: `docs/intellij-java-wooteco-style.xml`(우아한테크코스 스타일, 4스페이스/120열/연속 들여쓰기 8칸)로 `src` 하위 전체 자바 파일(98개, 85개 변경)을 IntelliJ CLI 포맷터로 재포맷, `./gradlew test` 통과 확인. 줄바꿈된 파라미터 목록도 표현식과 동일하게 8칸 연속 들여쓰기를 쓰는 것으로 정책 확정(사유는 `.harness/DECISIONS.md` 참조) — 코드 변경 없이 현재 상태 유지로 결론.

- Scrap CRUD API 구현(CLIAR-45): `.harness/DOMAIN.md`의 Scrap aggregate 규칙(LibraryBook을 통해서만 귀속, 독립 `memberId` 없음)을 그대로 구현했다. `Scrap`(JPA entity, aggregate root)이 `create`/`update`에 불변식(`sentence` 필수/공백 불가, `pageNumber`는 있으면 1 이상)을 캡슐화. `ScrapRepository`(포트)/`ScrapJpaRepository`/`ScrapRepositoryJpaAdapter`가 LibraryBook과 동일한 포트/어댑터 패턴을 따른다. `ScrapService`가 `LibraryBookRepository`를 함께 주입받아 책 스코프 endpoint(`createScrap`/`getScraps`)는 `LibraryBookNotFoundException`/`LibraryBookAccessDeniedException`으로, 스크랩 스코프 endpoint(`getScrap`/`updateScrap`/`deleteScrap`)는 스크랩→소속 책 조회 후 `ScrapNotFoundException`/`ScrapAccessDeniedException`으로 소유권을 검증한다(둘 다 CLIAR-29에서 이미 만들어둔 concrete 예외를 그대로 사용). `ScrapController`는 `/api/v1/library/books/{bookId}/scraps`와 `/api/v1/library/scraps/{scrapId}` 두 베이스 경로를 한 컨트롤러에서 메서드별 전체 경로로 처리(클래스 레벨 `@RequestMapping` 없음) — 이 저장소 최초의 다중 베이스 경로 컨트롤러. `V4__create_scrap.sql`로 `scrap` 테이블 신설, `book_id` FK에 `ON DELETE CASCADE`를 적용해 책이 삭제되면 스크랩도 함께 삭제되도록 했다(DOMAIN.md에 없던 결정이라 `.harness/DECISIONS.md`에 사유 기록). 목록 조회 정렬은 `createdAt` 오름차순(등록 순)으로 서버가 고정. 검증: `ScrapTest`(도메인 8개)/`ScrapServiceTest`(Mockito, 소유권 403/404 포함)/`ScrapControllerTest`(`@WebMvcTest`)/`ScrapRepositoryTest`(`RepositoryIntegrationTestSupport`, cascade 삭제 검증 — DB 레벨 cascade와 Hibernate 1차 캐시 불일치 문제를 `TestEntityManager.clear()`로 해결) 포함 `./gradlew check`(Docker Desktop 기동, 실제 PostgreSQL) 전체 통과. **(패키지 구조는 아래 항목에서 domain/application/infrastructure/web으로 재편됐다.)**

- `library` 패키지 계층 분리(도메인/애플리케이션/인프라/웹): 위 CLIAR-31/32/45에서 만들어진 `com.chc.dpgb.library` 평면 패키지(40개 파일)가 경계를 알아보기 어려워진 시점에, 동작 변경 없는 기계적 리팩터링으로 `domain`(엔티티·값객체 — `LibraryBook`/`Shelf`/`ShelfRank`/`ShelfRankExhaustedException`/`Scrap`) / `application`(유스케이스 서비스 + Repository 포트 — `LibraryBookService`/`ShelfService`/`ScrapService`, `LibraryBookRepository`/`ShelfRepository`/`ScrapRepository`, `LibrarySortBy`) / `infrastructure`(JPA 어댑터 — `*JpaRepository`/`*RepositoryJpaAdapter` 6쌍) / `web`(기존 그대로, Controller 3종 + dto)로 나눴다. 의존 방향은 `web → application → domain`, `infrastructure → application(포트 구현) + domain`. 대응하는 `src/test`/`src/integrationTest` 테스트도 같은 서브패키지로 함께 옮겼다. `./gradlew compileJava compileTestJava compileIntegrationTestJava`, `./gradlew test`, 이동된 Repository 통합 테스트 3개(`LibraryBookRepositoryTest`/`ShelfRepositoryTest`/`ScrapRepositoryTest`) 개별 실행 모두 통과 확인. 특정 Jira 티켓 없이 `CLIAR-45` 브랜치 위에서 진행하기로 사용자가 확인했다. `.harness/ARCHITECTURE.md`의 저장소 구조 절 갱신.

- Librarian API 구현(CLIAR-46): `.harness/DOMAIN.md`의 Librarian aggregate 규칙을 그대로 구현했다. `com.chc.dpgb.librarian` 패키지를 `library`와 동일한 4계층(domain/application/infrastructure/web)으로 신설. `Librarian`(JPA entity, 마스터 데이터 — Flyway 시드로만 채워지고 앱이 생성/수정하지 않아 도메인 메서드 없이 `@Getter`만, 테스트 픽스처용 public 전체-필드 생성자 하나만 예외적으로 둠)과 `MemberLibrarianSelection`(JPA entity, aggregate root — `memberId`(unique)/`librarianId`/`selectedAt`(`@UpdateTimestamp` — 최초 선택·재선택 모두 이 한 필드로 표현), `select(librarianId)`로 덮어쓰기). `LibrarianService`가 `getLibrarians`/`getMyLibrarian`(미선택 시 `LibrarianNotSelectedException`, 선택 시 `MemberLibrarianSelectionRepository`+`LibrarianRepository` 조합해 애플리케이션 계층 레코드 `SelectedLibrarian`으로 합성)/`selectMyLibrarian`(`librarianId` 미존재 시 `LibrarianNotFoundException`, 기존 선택 있으면 덮어쓰고 없으면 생성 — `ShelfService.getOrCreateDefaultShelf`와 동일하게 `saveAndFlush`+`DataIntegrityViolationException` 재조회로 동시 최초 선택 경합 처리)를 구현. `LibrarianNotSelectedException`/`LibrarianNotFoundException`은 CLIAR-29에서 이미 만들어져 있어 그대로 재사용(신규 예외 없음). `LibrarianController`가 `GET /api/v1/librarians`/`GET·PUT /api/v1/members/me/librarian`을 처리, DTO는 openapi 스키마 1:1 record. Flyway `V5__create_librarian.sql`로 `librarian`(마스터, PK만) + `member_librarian_selection`(`member_id` unique, `librarian_id` FK) 테이블을 만들고, 사용자가 지정한 시드 데이터(고양이=러시안블루, 새=슈빌, 둘 다 evolutionStage=1 — 진화 단계 자체는 후순위 개발로 보류)를 INSERT로 채웠다. 검증: `MemberLibrarianSelectionTest`(도메인 단위)/`LibrarianServiceTest`(Mockito, 미선택 404·미존재 404·재선택 덮어쓰기·동시 최초 선택 경합 포함)/`LibrarianControllerTest`(`@WebMvcTest`)/`LibrarianRepositoryTest`·`MemberLibrarianSelectionRepositoryTest`(`RepositoryIntegrationTestSupport`, 시드 데이터 조회·unique 제약 검증) 포함 `./gradlew check`(Docker Desktop 기동, 실제 PostgreSQL) 전체 통과.

- 계약 테스트 전수화(CLIAR-35): `openapi.yaml`의 22개 `operationId` × `responses` 조합 전부에 MockMvc(`@WebMvcTest`) 커버리지를 확보했다(ADR-0001 원칙). `LibraryBookControllerTest`/`ShelfControllerTest`/`ScrapControllerTest`(지금까지 성공 경로 + 컨트롤러 자체 400만 다루고 서비스가 던지는 403/404/409는 `*ServiceTest` Mockito 단위 테스트로만 검증했던 것)와 `BookDiscoveryControllerTest`에, 각 `@MockitoBean` 서비스가 해당 concrete 예외(`BookAlreadyRegisteredException`/`LibraryBookAccessDeniedException`/`LibraryBookNotFoundException`/`InvalidReorderTargetException`/`InvalidShelfDataException`/`ShelfAccessDeniedException`/`ShelfNotFoundException`/`DefaultShelfCannotBeDeletedException`/`InvalidScrapDataException`/`ScrapAccessDeniedException`/`ScrapNotFoundException`/`AladinApiException` 등)를 던지도록 `thenThrow`/`doThrow`로 주입하고 status + `jsonPath("$.code")`를 검증하는 테스트를 추가했다(`LibrarianControllerTest`의 기존 404 테스트 패턴 재사용). 500(`INTERNAL_ERROR`)은 계약에 없는 fallback이라 대상에서 제외(`GlobalExceptionHandlerTest`의 일반 검증으로 충분). 401은 모든 endpoint가 동일한 `SecurityFilterChain`을 타므로 operationId마다 반복하지 않고 컨트롤러 클래스당 대표 테스트 1개만 추가하기로 사용자에게 확인받아, 5개 컨트롤러 테스트 전부(Librarian은 기존에 이미 있었음) 대칭을 맞췄다. 컨트롤러 테스트 총 개수: `LibraryBookControllerTest` 29, `ShelfControllerTest` 15, `ScrapControllerTest` 18, `BookDiscoveryControllerTest` 5, `LibrarianControllerTest` 6. `./gradlew test` 전체 통과 확인.

- Swagger(API 문서 뷰어) 도입: `docs/api/openapi.yaml`을 브라우저에서 열람할 수 있게 `org.webjars:swagger-ui`(5.25.3, 순수 정적 HTML/JS)를 추가했다. springdoc-openapi 같은 애노테이션 기반 스펙 생성기 대신 정적 뷰어를 택한 이유: 이 저장소는 `docs/api/openapi.yaml`을 wire 계약의 유일한 소스로 못박아뒀는데(`CLAUDE.md` 단일 소유권 표), 애노테이션 기반 생성기는 코드에서 스펙을 만들어 소스가 둘로 나뉘고, 최신 버전이 이 저장소의 Spring Boot 4.1.0/Jackson 3 조합에서 검증되지 않은 리스크도 있었다(사용자에게 `AskUserQuestion`으로 확인받아 정적 뷰어로 확정). Gradle `processResources`에 `docs/api/openapi.yaml` → `static/openapi.yaml` 복사를 연결해 빌드 시점에 항상 원본과 동기화되도록 했고(수동 사본 없음), `src/main/resources/static/docs/index.html`이 webjar 자산을 로드해 `/openapi.yaml`을 렌더링한다. 모든 endpoint가 인증을 요구하는 기존 `SecurityConfig`(`anyRequest().authenticated()`)에서 사람이 보는 문서 페이지도 열람이 막히는 문제가 있어, `/docs/**`/`/webjars/**`/`/openapi.yaml` 세 경로만 `permitAll()` 예외를 추가했다(이 역시 사용자 확인). `SecurityConfigTest`에 문서 경로가 토큰 없이 200을 반환하는지 검증하는 테스트를 추가했다. `./gradlew test` 통과 확인 + `./gradlew bootRun`(Docker Postgres 기동)으로 실제 `/docs/index.html`, `/openapi.yaml`, `/webjars/...`가 200을, 실제 API(`/api/v1/librarians`)는 여전히 401을 반환하는 것을 curl로 직접 확인했다.

- 대표 사서 선택을 Member 서비스로 이관(ADR-0009): 사용자가 검토한 Member 서비스 ERD 초안에 `representative_librarian_id`가 있어, Book Service의 `member_librarian_selection`과 같은 사실이 두 서비스에 중복 저장되는 문제를 확인했다. "대표 사서 선택"은 도서 관리와 무관한 회원 프로필 성격이라 Member 서비스 소유로 정리하고, Book Service는 사서 마스터 카탈로그(`getLibrarians`)만 유지한다. `docs/api/openapi.yaml`(v0.7.0)에서 `getMyLibrarian`/`selectMyLibrarian`, `MyLibrarianResponse`/`SelectLibrarianRequest`, `LibrarianNotSelected`/`LibrarianNotFound`를 제거(`LibrarianType`/`LibrarianSummary`/`LibrarianListResponse`는 `getLibrarians`가 계속 사용해 유지). `com.chc.dpgb.librarian`에서 `MemberLibrarianSelection`(domain)/포트+JPA 어댑터(application/infrastructure)/`LibrarianService.getMyLibrarian`·`selectMyLibrarian`/`LibrarianController`의 해당 메서드/`MyLibrarianResponse`·`SelectLibrarianRequest`(dto)를 삭제, `LibrarianNotSelectedException`/`LibrarianNotFoundException`(다른 곳에서 안 쓰여 함께 삭제 확인)도 제거. `V6__drop_member_librarian_selection.sql`로 테이블 DROP(`V5`는 이미 병합되어 직접 수정하지 않음, `librarian` 마스터·시드는 유지). `MemberLibrarianSelectionTest`/`MemberLibrarianSelectionRepositoryTest` 삭제, `LibrarianServiceTest`/`LibrarianControllerTest`를 카탈로그 조회 케이스만 남기도록 축소. `docs/db/erd.dbml`에서 `member_librarian_selection` 제거, `librarian.type`을 DBML enum(`CAT`/`BIRD`)으로 변경. `.harness/DOMAIN.md`(대표 사서 선택 규칙을 Member 서비스 소유로 명시), `.harness/ARCHITECTURE.md`(librarian 패키지 구조), `docs/api/README.md`(ADR-0009 목록), `.harness/BACKLOG.md`(탈퇴 회원의 Book Service 데이터 처리는 지금 손대지 않음) 반영. `./gradlew check`(Docker Desktop 기동, 실제 PostgreSQL — V6 DROP TABLE 마이그레이션 포함) 전체 통과 확인.

- DB 스키마 대개편 목표 스키마 문서화(2026-08-25, 당시 아직 코드/마이그레이션 미적용): `docs/db/erd.dbml`을 목표 스키마로 전체 재작성했다. 결정 내용·사유는 `.harness/DECISIONS.md`(2026-08-25 두 항목) 참조.

- DB 스키마 대개편 구현(V7~V9, soft delete, librarian 전면 재설계): 위 목표 스키마를 실제 코드로 구현했다. Flyway `V7__rescope_shelf_and_library_book.sql`(shelf/library_book PK를 `id`로 리네이밍, `member_id` UUID화, `deleted_at` 추가, `genre_type`/`book_reading_status` 네이티브 enum 생성 + `genre`/`reading_status` 컬럼 추가, `total_pages` nullable화, `cover_url` TEXT화), `V8__rescope_scrap.sql`(PK 리네이밍, `deleted_at`, `scrap_image_url` 필수 컬럼 추가 — 기존 행은 빈 문자열로 백필 후 NOT NULL 전환, `book_id` FK의 `ON DELETE CASCADE` 제거), `V9__redesign_librarian.sql`(기존 마스터 카탈로그 `librarian` 테이블 DROP 후 `librarian_type` enum→`librarian_type_info`→`librarian_level`→`librarian` 순으로 재생성 — 사용자 제공 SQL의 테이블 생성 순서 오류를 바로잡음, `librarian_level`은 `level=1,required_experience=0` 최소 시드만).

  엔티티: `Shelf`/`LibraryBook`/`Scrap`/`Librarian`(4개 모두) `memberId`를 `UUID`로, `deletedAt`+`softDelete(Instant)`+`isDeleted()` 추가. Soft delete는 각 엔티티에 `@SQLRestriction("deleted_at IS NULL")`(Hibernate 6.3+, `@Where`의 후속)을 붙여 모든 조회 쿼리(Spring Data 파생 쿼리·JPQL·`findById` 포함)에 자동 적용했다 — 서비스/리포지토리 코드에서 조건을 반복하지 않아도 된다. `MemberIdResolver.resolve`도 `UUID.fromString(jwt.getSubject())`로 변경(Cognito sub가 UUID 형식이라는 전제, 유효하지 않으면 `IllegalArgumentException`). `LibraryBook`에 `Genre`(16종)/`ReadingStatus`(`PLANNED`/`READING`/`COMPLETED`, ADR-0005가 제거했던 것과 동일한 개념이지만 값 재정의) enum 필드 추가 — Postgres 네이티브 enum은 `@Enumerated(STRING) + @JdbcTypeCode(SqlTypes.NAMED_ENUM)`으로 매핑한다(이 저장소 최초 네이티브 enum 사용, `LibrarianType`에도 동일 패턴 적용). `total_pages`가 nullable로 바뀌면서 `LibraryBook.totalPages`를 `Integer`로, `register`/`updateMetadata`/`updateProgress`/`progress()`를 null-허용으로 다시 설계했다 — `totalPages`가 없으면 `progress()`는 `null`을 반환한다(도메인 결정, DOMAIN.md 반영 예정). `Librarian`을 마스터 카탈로그 엔티티에서 회원 소유 인스턴스 엔티티(`memberId`/`type`/`name`/`level`/`experience`/`isRepresentative`, `acquire`/`rename`/`markAsRepresentative`/`unmarkAsRepresentative` 도메인 메서드)로 전면 재작성. 신규 `LibrarianTypeInfo`(마스터, Flyway 시드 전용) 엔티티 추가 — **`LibrarianLevel` 엔티티는 만들지 않았다**: 현재 앱 코드 어디서도 레벨 정책 값을 조회/사용하지 않아(레벨업 로직은 이번 범위 밖) JPA 매핑 없이 DB 테이블/FK만 존재하는 상태로 남겨뒀다(YAGNI, 레벨업 API를 만들 때 추가).

  리포지토리(포트+JPA 어댑터): 4개 aggregate 모두 하드 `delete()` 포트 메서드를 제거하고 서비스가 `softDelete(Instant.now())+save()`를 호출하는 방식으로 통일. `ScrapRepository`에 `findAllByBookId`(캐스케이드 soft delete용) 추가. `Librarian`/`LibrarianTypeInfo` 신규 포트+어댑터(`existsByMemberIdAndType`, `findRepresentative` 등).

  서비스: `ShelfService.deleteShelf`는 기존과 동일하게 책을 기본 책장으로 이동시킨 뒤 그 Shelf만 soft delete. `LibraryBookService.deleteLibraryBook`은 soft delete 후 `ScrapService.softDeleteAllByBookId`(package-private, 같은 `library.application` 패키지에서만 호출)를 호출해 소속 Scrap 전체를 벌크 soft delete — 기존 DB `ON DELETE CASCADE`(CLIAR-45)를 애플리케이션 오케스트레이션으로 대체. `LibrarianService`를 전면 재작성해 신규 API 7종을 구현: `getLibrarianTypes`/`acquireLibrarian`(타입 중복 시 409 `LibrarianAlreadyOwnedException`)/`getLibrarians`/`renameLibrarian`/`selectRepresentative`(기존 대표가 있으면 해제 후 대상 지정, DB `uk_librarian_member_representative` 부분 unique 인덱스로 동시성 보장)/`getRepresentative`(미선택 시 404 `RepresentativeLibrarianNotSelectedException`)/`deleteLibrarian`. 신규 concrete 예외 5종(`LibrarianNotFoundException`/`LibrarianAccessDeniedException`/`LibrarianAlreadyOwnedException`/`InvalidLibrarianDataException`/`RepresentativeLibrarianNotSelectedException`).

  컨트롤러/DTO: `LibraryBookController`/`ShelfController`/`ScrapController`의 `memberId` 지역변수를 `UUID`로, `LibraryBook` 관련 DTO(`Create`/`UpdateLibraryBookRequest/Response`, `LibraryBookSummary`/`DetailResponse`, `UpdateReadingProgressResponse`)에 `genre`/`readingStatus` 필드와 nullable `totalPages`/`progress` 반영. `Scrap` 관련 DTO에 `scrapImageUrl` 필드 추가. `LibrarianController`를 `GET /api/v1/librarian-types`/`POST,GET /api/v1/librarians`/`PATCH /api/v1/librarians/{id}`/`PATCH,GET /api/v1/librarians/{id 또는 없음}/representative`/`DELETE /api/v1/librarians/{id}` 7개 endpoint로 전면 재작성(기존 `getLibrarians` 단일 endpoint 대체).

  테스트: soft delete/UUID/genre·readingStatus/scrapImageUrl 반영해 기존 도메인·서비스·컨트롤러·리포지토리 테스트 전부 갱신(주로 `"member-1"` 문자열을 `UUID.randomUUID()` 상수로 교체하는 기계적 작업). `LibraryBookServiceTest`에 책 삭제 시 스크랩 벌크 soft delete 호출 검증 추가, `ScrapServiceTest`에 `softDeleteAllByBookId` 단위 테스트 추가. 기존 `ScrapRepositoryTest`의 "책 삭제 시 스크랩 DB cascade 삭제" 테스트는 `ON DELETE CASCADE` 제거로 전제 자체가 사라져 제거하고 `findAllByBookId` 조회 테스트로 대체. `LibrarianRepositoryTest`/신규 `LibrarianTypeInfoRepositoryTest`를 새 모델 기준으로 재작성(구 마스터 카탈로그 시드 조회 테스트는 전부 폐기 — V9가 그 테이블을 DROP했으므로). `./gradlew check`(Docker Desktop 기동, 실제 PostgreSQL, V7~V9 마이그레이션 포함) 전체 통과 확인.

  아직 안 한 것: `docs/api/openapi.yaml`/`.harness/DOMAIN.md`/`ARCHITECTURE.md`/신규 ADR/`BACKLOG.md` 문서 동기화 — `.harness/PLAN.md` 참조.

- DB 스키마 대개편 문서 동기화: `docs/api/openapi.yaml`(v0.8.0)을 신규 스키마·API 표면에 맞춰 갱신 — `Genre`/`ReadingStatus` 스키마 신설, `LibraryBook` 관련 요청/응답 스키마에 `genre`/`readingStatus` 반영·`totalPages`를 선택 필드로 전환, `Scrap` 관련 스키마에 `scrapImageUrl` 필수 필드 추가, `Librarian` 관련 스키마·엔드포인트를 7종(`getLibrarianTypes`/`acquireLibrarian`/`getLibrarians`/`renameLibrarian`/`selectRepresentative`/`getRepresentative`/`deleteLibrarian`)으로 전면 교체, 관련 오류 응답 6종 추가(`InvalidLibrarianData`/`LibrarianAlreadyOwned`/`LibrarianAccessDenied`/`LibrarianNotFound`/`RepresentativeLibrarianNotSelected`). 파이썬 스크립트로 `$ref` 무결성·중복 operationId·미사용 schema/response/parameter 재검증 완료(operationId 26개, 이슈 없음). `docs/api/decisions/0010-reintroduce-genre-and-reading-status.md`(ADR-0010, ADR-0003 genre 부분·ADR-0005 전체 반전)과 `0011-librarian-ownership-model.md`(ADR-0011, ADR-0009 대체) 신설, ADR-0003/0005/0009 각 문서에 append-only 각주("이후 반전됨")로 상호 참조 추가, `docs/api/README.md` ADR 목록 갱신. `.harness/DOMAIN.md`에 논리 삭제 공통 규칙 절 신설, `LibraryBook`/`Shelf`/`Scrap` aggregate 절에 soft delete·genre·readingStatus·scrapImageUrl 반영, `Librarian` 절을 회원 소유 인스턴스 모델로 전면 재작성, ADR-0010/0011 "결정된 사항" 절과 "미결정 도메인"(레벨 정책 값) 추가. `.harness/ARCHITECTURE.md`에 soft delete/`@SQLRestriction`/네이티브 enum 매핑 컨벤션, V7~V9 마이그레이션 목록, librarian 패키지 구조, `MemberIdResolver` UUID 반환, 갱신된 테스트 트리 반영. `.harness/BACKLOG.md`에 `librarian_level` 정책 값 미정·레벨업 로직 설계 필요 항목 추가. `docs/db/erd.dbml` 헤더 주석을 "목표 스키마(미구현)"에서 실제 V1~V9 적용 후 스냅샷으로 갱신(본문 스키마 정의는 이미 구현과 일치해 별도 수정 불필요).

## 미완료 / 진행 중

`.harness/PLAN.md` 참조.
